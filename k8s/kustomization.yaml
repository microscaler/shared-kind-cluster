# Order: StorageClass → Supabase (data) → platform-data (pipeline, redis, minio, …) → observability
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - storageclass-local.yaml
  - ../../microscaler-supabase/k8s/overlays/shared-kind
  - platform-data
  - observability
