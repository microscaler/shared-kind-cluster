apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mailpit-storage-pv-claim
  namespace: data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  resources:
    requests:
      storage: 1Gi
