# Dev defaults (matches local Kind); align with CI by using the same values as GitHub `PGPASSWORD` when testing parity.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: data
type: Opaque
stringData:
  postgres-password: postgres
  postgres-replication-password: postgres
