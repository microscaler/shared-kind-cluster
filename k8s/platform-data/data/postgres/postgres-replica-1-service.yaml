apiVersion: v1
kind: Service
metadata:
  name: postgres-replica-1
  namespace: data
  labels:
    app: postgres-replica-1
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
  selector:
    app: postgres-replica-1
