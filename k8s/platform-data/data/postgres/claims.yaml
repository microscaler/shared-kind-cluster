apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pact-postgres-data
  namespace: data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  resources:
    requests:
      storage: 1Gi
