apiVersion: v1
kind: Service
metadata:
  name: postgres-primary
  namespace: data
  labels:
    app: postgres-primary
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: postgresql
      protocol: TCP
  selector:
    app: postgres-primary
