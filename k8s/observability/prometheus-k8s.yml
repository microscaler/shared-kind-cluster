# Scrape the in-cluster OpenTelemetry collector (metrics exporter on :9464).
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:9464"]
